This directory should contain textures, audio, etc. that you would like to load in using the ContentsManager.

The CopyRef.ps1 build script will manage copying the contents over to the destination so that you don't have to add them to the project.